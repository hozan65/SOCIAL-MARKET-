# netlify.toml

[build]
command = ""
publish = "."

[functions]
directory = "netlify/functions"
node_bundler = "esbuild"

# (Opsiyonel) Scheduled function kullanıyorsan:
[functions."news_ingest"]
schedule = "*/5 * * * *"
